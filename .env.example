# Copy to .env and fill values from Supabase → Project Settings → API
# Dev server uses port 8080 (see vite.config.ts). In Supabase → Authentication
# → URL Configuration set Site URL and Redirect URLs to http://localhost:8080

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
